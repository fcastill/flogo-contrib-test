{
  "name": "fcastill-timer",
  "type": "flogo:trigger",
  "version": "0.10.0",
  "title": "Mock Timer",
  "description": "Simple Timer trigger",
  "homepage": "https://github.com/fcastill/flogo-contrib-test/tree/master/trigger/timer",
  "display": {
    "icon": "clock.svg"
  },
  "handler": {
    "settings": [
      {
        "name": "startDelay",
        "type": "string",
        "description": "The start delay (ex. 1m, 1h, etc.), immediate if not specified"
      },
      {
        "name": "repeatInterval",
        "type": "string",
        "description": "The repeat interval (ex. 1m, 1h, etc.), doesn't repeat if not specified"
      }
    ]
  }
}
